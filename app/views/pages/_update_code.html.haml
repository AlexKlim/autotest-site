%label
  Additional settings:

= simple_form_for Environment.new, url: update_code_path do |f|
  = simple_fields_for :test do |sf|
    = sf.select :tag, Tag.all.map(&:name)

    %div
  = f.submit 'Update'

= simple_form_for :update_test, url: update_test_path do |f|
  = f.hint 'Update Tests:'
  = f.submit 'Update'

= simple_form_for :update_tag, url: update_tag_path do |f|
  = f.hint 'Update Tags:'
  = f.submit 'Update'